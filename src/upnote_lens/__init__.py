"""upnote-lens-mcp — a lens for looking into UpNote.

Reads come from the local UpNote SQLite database (read-only); writes go through
the ``upnote://`` URL scheme (x-callback-url).
"""

__version__ = "0.1.1"
